{
  "name": "domestic-intelligence-web",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-ui.mjs",
    "build:check": "node scripts/build-ui.mjs --check",
    "typecheck": "tsc -p tsconfig.v07.json",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/verify-browser-v07.mjs",
    "verify": "npm run build:check && npm run typecheck && npm test && npm run test:browser"
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "typescript": "5.9.3"
  }
}
